## 1. Модель и контракт записи

- [x] 1.1 Добавить в backend нормализацию `preassessment` с полями `riskRows`, `riskBaseUrl`, `summaryDecision`, `alexanderDecision`, `estimateFileUrl`.
- [x] 1.2 Добавить нормализацию строк риска: `order`, `parameter`, `managerComment`, `criticality`, `riskBaseRef` или `sourceKey`.
- [x] 1.3 Поддержать статусы критичности `critical`, `notCritical`, `unknown` и русские входные значения `Критично`, `Не критично`, `Не указано`.
- [x] 1.4 Поддержать итоговые значения `summaryDecision` и `alexanderDecision`: `estimate` (`Оценка`) и `decline` (`Не участвуем`).
- [x] 1.5 Расширить patch/save контракт записи так, чтобы `preassessment` сохранялся без потери существующих полей.
- [x] 1.6 Проверить, что старые записи без `preassessment` открываются и сохраняются с безопасным пустым состоянием.

## 2. UI блока предоценки

- [x] 2.1 Добавить состояние формы для `preassessment` на странице scoring-записи.
- [x] 2.2 Добавить компонент блока `Предоценка` ниже секции `Критерии выбора`.
- [x] 2.3 Реализовать табличные строки риска с колонками `Параметр`, `Комментарий менеджера и/или предложение о том можем ли обойти данный риск`, `Критично/Не критично`.
- [x] 2.4 Добавить ручное добавление, редактирование и удаление строк риска с пересчетом порядка.
- [x] 2.5 Добавить select для критичности и визуальное состояние незаполненной критичности.
- [x] 2.6 Добавить фиксированную внешнюю ссылку `База рисков` для таблицы рисков без отдельного input.
- [x] 2.7 Добавить итоговые поля `Решение по итогам предоценки`, `Решение Александра`, `Файл оценки`, хранящиеся отдельно от строк риска.
- [x] 2.8 Ограничить `Решение по итогам предоценки` и `Решение Александра` значениями `Оценка` и `Не участвуем`.

## 3. Runtime и внутренний контракт

- [x] 3.1 Убедиться, что `preassessment` возвращается в API записи как структурированный объект.
- [x] 3.2 Убедиться, что web-карточка использует structured-данные блока без ручного парсинга текста.
- [x] 3.3 Не добавлять генерацию Excel-файла или Excel-export в первый implementation pass.
- [x] 3.4 Обновить AI-pass/scoring payload только если текущий runtime уже передает аналогичные structured-блоки в контракт.

## 4. Проверки и приемка

- [x] 4.1 Добавить unit/smoke-проверку нормализации `preassessment` и статусов критичности.
- [x] 4.2 Добавить проверку сохранения и повторного открытия карточки с заполненным блоком предоценки.
- [x] 4.3 Проверить responsive-вид блока на desktop и mobile, чтобы длинный комментарий не ломал строку и не перекрывал контролы.
- [x] 4.4 Прогнать существующие проверки проекта: `npm run check:encoding`, релевантный smoke/test для записи и frontend build.
- [x] 4.5 Провести QA-pass по сценариям: пустой блок, ручное добавление риска, выбор `Не критично`, заполнение `Оценка` / `Не участвуем`, сохранение и повторное открытие.
