{
  "name": "scoring-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=../.env.local --env-file-if-exists=.env.local --watch src/server.js",
    "start": "node --env-file-if-exists=../.env.local --env-file-if-exists=.env.local src/server.js"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "express": "^4.21.2",
    "multer": "^2.0.2"
  }
}
