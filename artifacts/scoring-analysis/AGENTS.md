# AGENTS.md

Отвечай по-русски, если пользователь явно не просит другой язык.

## Граница проекта

`C:\Users\illki\Desktop\projects\scoring-analysis` — отдельный проект тестовой подсистемы анализа документов.

Не смешивать его runtime, storage и доменную логику с `scoring/`.

`scoring-analysis` является заменяемым downstream-сервисом:

- владеет распаковкой архивов;
- владеет нормализацией документов в MD;
- владеет классификацией документов;
- владеет извлечением фактов;
- возвращает результат через HTTP API.

`scoring/` должен обращаться сюда только через API-контракт.
